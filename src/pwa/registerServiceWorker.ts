/**
 * PWA — placeholder. There is no manifest, service worker asset, or
 * vite-plugin-pwa config in this project yet (a real PWA pass is tracked as
 * separate, not-yet-scoped work), so this deliberately does nothing rather
 * than registering a service worker file that doesn't exist. Called
 * unconditionally from main.tsx so wiring the real thing in later needs no
 * call-site change.
 */
export function registerServiceWorker(): void {}
