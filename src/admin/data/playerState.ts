import type { AdminPlayerStateItem } from '../types'

/** No save/load system exists yet. Returns an empty read model until one is built. */
export function getPlayerStateItems(): AdminPlayerStateItem[] {
  return []
}
